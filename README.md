# МАШИНКИ — игра для малышей (3 года)

Демо: выбор машинки → выбор режима → бесконечная поездка. Всё управление — **ПРОБЕЛ** и **← →**.

## Запуск

Онлайн: **https://managersergei.github.io/babygame/** (GitHub Pages, деплой из `main` через `.github/workflows/pages.yml`).
Пульт: https://managersergei.github.io/babygame/pult.html

Локально:

```bash
cd ~/Documents/babygame
python3 -m http.server 8765
```

Открыть http://127.0.0.1:8765/ (можно и просто двойным кликом по `index.html`).

## Экраны

1. **Гараж** — `← →` листают 10 машинок, `ПРОБЕЛ` — дальше.
2. **Режим** — `← →` выбирают, `ПРОБЕЛ` — поехали:
   * **ТОЛЬКО ПРОБЕЛ** — машинка едет сама, ребёнку нужна одна кнопка;
   * **СО СТРЕЛКАМИ** — `→` газ, `←` тормоз, `ПРОБЕЛ` прыжок.
3. **Поездка** — звёзды, шарики, конусы, лужи, трамплины, радужная арка каждые 10 звёзд.

**Бензин.** Тратится по мере езды, канистры лежат по дороге (гарантированно раз в ~1100 px).
Кончился — машинка мягко останавливается, экран «БЕНЗИН ЗАКОНЧИЛСЯ», `ПРОБЕЛ` — заправиться и ехать дальше.
Проигрыша нет, звёзды не сгорают.

Кнопки внизу дублируют клавиши (для тача). Мелкие кнопки справа снизу: 🏠 гараж, 🔊 звук (клавиша `M`), ⛶ полный экран.
`Esc` — вернуться в гараж.

## Режим РУЛЬ и телефон-руль

Третий пункт на экране «Сюжет / Бесконечный / **Руль**»: дорога от третьего лица, три полосы,
`← →` (или кнопки внизу) — руление, `ПРОБЕЛ` — гудок и ускорение. Проигрыша нет: препятствие —
«ой» и замедление, звёздочка — «+1», каждые 10 — похвала. Вид машинки сзади пока рисуется кодом
(рисованные спрайты «сзади» — после согласования).

**Телефон как руль.** В режиме РУЛЬ игра показывает код из 4 знаков и QR; на телефоне открыть
`pult.html`, ввести код — кнопки ◀ ▶ ⬆ и наклон телефона (кнопка «Наклон — руль», на iPhone
спросит разрешение; наклон работает только по HTTPS). Связь — WebRTC через PeerJS (публичный
сигналинг), поэтому нужен интернет и сайт по HTTPS. Пульт работает и в других режимах
(⬆ = пробел).

## Для родителей, PWA, аналитика

* При первом запуске — экран «Родителям — на 20 секунд» (кнопка ⓘ открывает снова).
* `manifest.json` + `sw.js`: «добавить на экран», офлайн-кэш (service worker включается только по HTTPS).
* Аналитика: константа `YM_ID` в `index.html` (0 — выключено); события `start`, `stage_done`,
  `over`, `lane_hit`, `parent_ok`, `pult_on` уходят в Яндекс Метрику как цели.
* На экранах уже 1000 px кнопки-инструменты уезжают наверх, в портрете показывается «поверни телефон».

## Отладка

`index.html?s=garage|mode|play|empty|name|lesson|repair|camp` — сразу нужный экран.
Ещё: `&dbg=1` — оверлей со стейтом и трассой переходов, `&fast=N` — ускорение времени в N раз,
`&w=jump|fuel|light` — какой урок, `&st=N` — с какого уровня кампании, `&ph=` — фаза мастерской.
Для роликов: `&rec=1` — без кнопок и музыки, реплики и события в консоль (`EV t name`), `&auto=1` — машинка
играет сама, `&seed=N` — повторяемая дорога, `&test=1` — `window.__bg()` со состоянием для автотестов.

Автопроверки: `python3 -m http.server 8765 &` → `node tools/smoke.mjs` (гараж → руль, тач в портрете,
запись). Сверка озвучки: `python3 tools/check_voice.py`. Иконки и OG: `python3 tools/make_icons.py`.

## Ролики

`reels/` — сцены для вертикальных роликов кодом (скилл `code-motion`): `node reels/build.mjs [01 02 …] [--quick]`
рендерит через виртуальное время сразу в mp4, подкладывает реплики диктора по логу событий и нормализует
звук. `reels/storyboards/` — сториборды ИИ-историй (`gen.py`, fal.ai). Планы — `ПРОДВИЖЕНИЕ.md`, `РИЛСЫ-20.md`.

## Графика

Спрайты сгенерированы через fal.ai (`flux/dev`), фон и запечённая тень вырезаны скриптом.
Игра грузит `.webp` (из PNG: `cwebp -q 88`), музыку — `.mp3`; PNG и WAV остаются на диске, но не в git.

```bash
export BABYGAME_FAL_KEY='...'
python3 tools/gen_sprites.py            # всё, что ещё не скачано в assets_raw/
python3 tools/gen_sprites.py fire star  # только эти
```

* `assets_raw/` — исходные PNG от нейросети (не удалять: повторная обработка идёт из них, без новых запросов к API);
* `assets/` — обрезанные прозрачные спрайты, их грузит игра.

Звук целиком синтезируется в WebAudio (мотор, сирена, гудок, монетки), голос — `speechSynthesis`
(если в системе нет русского голоса, озвучка молча отключается).

## Голосовой режим

Третий режим на экране выбора. Порядок уровней и слов — в **[LEVELS.md](LEVELS.md)**.

Кратко: знакомство («как тебя зовут?» ×3) → короткая гонка, где учим **ПРЫЖОК** у ямы →
мастерская с вопросом «сколько колёс?» → гонка со словом **БЕНЗИН** → мастерская →
гонка со словом **ФАРА** → и так по кругу. Одно новое слово за этап; выученное слово
сразу работает как живая команда.

Распознавание своё, офлайн: игра запоминает, как слово звучит у конкретного ребёнка
(спектр + DTW), а не пытается разобрать русскую речь. Образцы — в `localStorage`.
Микрофон спрашивается при первом нажатии клавиши; отказ или кнопка 🚫 просто отключают
голос — всё проходится пробелом.

## Дальше в планах

* короткое объяснение перед каждым уровнем — голосом и текстом («это первый уровень,
  сейчас машина поедет, и мы научим её прыгать, ты готов?»);
* следующие слова: СТОП, НАПРАВО, НАЛЕВО, числа;
* подкалибровка на ходу, когда слово перестало узнаваться.
