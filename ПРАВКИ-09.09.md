# Правки бесконечного режима, 09.09.2026

16 пунктов владельца. Ниже — что сделано, как проверено и что осталось решить.

| # | Что просили | Статус | Как проверено |
|---|---|---|---|
| 1 | Новые слова | ✅ 18 слов: монета, сундук, овечка, трактор, автобус, скорая, экскаватор, мусоровоз, эвакуатор, башня, магазин, пальма, зонтик, обезьяна, камень, ёлка, снеговик, мастер | 54 реплики озвучены бесплатным edge-tts; все 18 спрайтов грузятся; слово входит в очередь только с полной озвучкой |
| 2 | Новое слово раз в 15–30 с | ✅ таймер `wordT`, заказ предмета дороге, принудительная постановка через 5 с | `tools/rhythm-test.mjs`: паузы 16 · 27 · 18 · 26 с при чистом заезде; длиннее — только когда между словами случилась поломка |
| 3 | Конус и лужа слишком близко | ✅ правило `TUNE.minGap` (430 px) на все препятствия и переезд; ряд из трёх конусов → два | расчёт дальности прыжка при разных скоростях; в `__bg().minGap` минимальный зазор в кадре |
| 4 | Монстрики во время погони | ✅ первые две погони и 6 с до них — без монстриков, не вошедшие в кадр убираются | `rhythm-test`: ноль замеров с монстриками во время первых погонь |
| 5 | Повреждённый монстр-трак смотрит влево | ✅ развёрнут `monster_d3` (файл, не код) | сравнение с базой и зеркалом по всем 48 вариантам машин |
| 6 | Повреждение меняет цвет | ✅ тон повреждённых подогнан под базу по каналам | средний цвет в маске: было −20 %, стало ±4 % |
| 7 | 4 колеса почти не ломаются | ✅ каждая третья случайная поломка — все четыре; поезд сносит все | `?wn=4` для ручной проверки, `window.__roll()` в тестовом режиме |
| 8 | Скрыть режим уровней | ✅ «Сюжет» скрыт, остались «Бесконечный» и «Руль»; `?all=1` возвращает | headless: миграция старого выбора, защита от несуществующего режима |
| 9 | Скрыть голосовое управление | ✅ пункт «Голосом» скрыт, упражнения (имя, гараж, уроки, команды) работают от кнопки 🎤 | headless: урок слова случается без режима «Голосом» |
| 10 | Сохранение прогресса | ✅ выученное не учится заново и сразу работает командой; кнопка сброса | `__bg().word.next` = null при заполненной статистике |
| 11 | Прослушивание записей | ✅ вторая попытка со звуком пишется в WAV и хранится на устройстве; в панели ▶ ⬇ ✕, галка и лимит | `tools/rec-test.mjs`: запись создаётся, играет, удаляется; на живом голосе и iOS не проверено |
| 12 | Объекты неестественны | ⏳ шаг 1: контактные тени, глубина, покачивание, дальний ряд мельче | автотесты проходят; вид оценивает владелец |
| 13 | Поезд слишком быстрый | ✅ состав из локомотива и 2–4 вагонов, выезжает под приезд машинки, его надо перепрыгнуть (+3 звезды) | `__bg().rail` показывает вагоны и занятость полотна |
| 14 | Робот каждые 45–75 с, когда слов нет | ✅ таймер идёт только при пустой очереди слов | `tools/robot-test.mjs` с заполненной статистикой |
| 15 | Автогенерация спрайтов в будущем | ✅ готов роутер бесплатных генераций `~/Documents/freegen` (302 теста, живая проверка) | — |
| 16 | QR и ссылка на Telegram-бота | ✅ экран для родителей + реле на Cloudflare Worker в `tg/` | без `TG_BOT`/`TG_API` блок скрыт и ничего не отправляется |

## Что нужно от владельца

1. **Телеграм-бот.** Создать бота у @BotFather и аккаунт Cloudflare, выполнить пять команд из `tg/README.md`,
   прислать username бота и адрес воркера — я впишу их в две константы.
2. **Анимация окружения (пункт 12, шаг 2).** Предлагаю три кадра на объект для десяти живых объектов:
   корова, лошадь, курица, овечка, ёжик, обезьяна, мельница, бабочка, птица, монета. Кадры делает
   `freegen` бесплатными провайдерами, вес около 40 КБ на объект. Нужно ваше «да» — это правка контента.
3. **Проверить на живом ребёнке**: интервал слов, поезд (перепрыгивается ли), запись голоса и её
   разборчивость, новые слова.
4. Диктор теперь смешанный: старые 346 реплик — прежний голос, 54 новых — бесплатный. Если резать слух,
   перегенерирую всё одним голосом одной командой.
