/* Настройки игры, выгруженные админкой (панель внутри игры → «Выгрузить tune.js»).
   Пусто — игра берёт заводские значения из кода. Файл коммитится: то, что здесь,
   получают все дети после публикации. */
window.TUNE_OVERRIDES = {};
